# EP001 — Silent listening stitch notes

Use when a guest speaks and Pomplarion stays on camera **without talking**.

## Render

Generate **each** base listen clip once at **8s** (L01 → L02 → L03 → L04 → L05 → L06).
Re-use and loop in edit to reach **22 clips** total for **170 (2:50 — trim from 22×8s)**.
Paste `EP001-Lxx.json` into Grok Imagine with matching reference PNG.

## Stitch in edit

- Cut order: Loop **L01 → L02 → L03 → L04 → L05 → L06** until **22 clips** (22 × 8s = 176s)
- Example start: L01 → L02 → L03 → L04 → L05 → L06 → L01 → L02 → … (repeat pattern)
- Cut on **blink** or **micro-nod** for invisible joins
- Target total hold: **170 (2:50 — trim from 22×8s)**
- Audio: **guest only** — mute Pomplarion track

## Context

After MS11, loop L01→L06 in order until 22 clips (176s), then trim tail to 2:50. Guest audio only.

## All listen clips

- `EP001-L01.json` + ref from listening-scene-map
- `EP001-L02.json` + ref from listening-scene-map
- `EP001-L03.json` + ref from listening-scene-map
- `EP001-L04.json` + ref from listening-scene-map
- `EP001-L05.json` + ref from listening-scene-map
- `EP001-L06.json` + ref from listening-scene-map
